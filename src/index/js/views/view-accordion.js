import ViewBase from './view-base';
import '../../../components/accordion/accordion.scss';

class AccordionView extends ViewBase {
    constructor(hash) {
        super(hash);
    }
}

export default AccordionView;
